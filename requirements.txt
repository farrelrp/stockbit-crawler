# Core Flask dependencies
Flask==3.0.0
requests==2.31.0
Werkzeug>=3.0.0

# Data processing
pandas==2.1.3
pyarrow>=14.0.0

# WebSocket and async
websockets==12.0
tornado>=6.4

# Perspective for market replay (requires Python 3.9+)
# Install with: pip install perspective-python
perspective-python>=2.10.0

# Telegram bot
python-telegram-bot>=21.0

# Timezone support
pytz>=2023.3

# Environment variables
python-dotenv>=1.0.0

# Google Drive upload (service account)
google-api-python-client>=2.100.0
google-auth>=2.23.0

# Auto-login only needs requests (already listed above) + 2Captcha API
# No browser automation required — reCAPTCHA v3 is solved server-side
